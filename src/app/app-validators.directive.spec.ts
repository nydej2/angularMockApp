import { AppValidatorsDirective } from './app-validators.directive';

describe('AppValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
